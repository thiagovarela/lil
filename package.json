{
  "name": "lil-workspace",
  "private": true,
  "workspaces": [
    "packages/lil",
    "packages/web"
  ],
  "scripts": {
    "dev": "bun run --filter lil dev",
    "web:dev": "bun run --filter lil-web dev",
    "web:build": "bun run --filter lil-web build"
  }
}
